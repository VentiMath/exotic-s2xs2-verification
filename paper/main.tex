% Independent machine verification of the fundamental-group computation in
% Wuebben's exotic S^2 x S^2 — arXiv verification note.
%
% Numbers are taken from the imported certificates in this repository
% (verification/). Every bibliography entry is verified against the paper's
% own reference list (arXiv:2608.17267, printed pages 31-32) or the arXiv
% API; none is reconstructed from memory.
\documentclass[11pt]{article}
\usepackage[margin=1.15in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{microtype}
\usepackage[hidelinks]{hyperref}
% URLs wrap at slashes and dots only, never between the scheme and its
% slashes, and never with an inserted hyphen.
\DeclareUrlCommand\repopath{\urlstyle{tt}\def\UrlBreaks{\do\/\do\_\do\-\do\.}}
\def\UrlBreakPenalty{0}\def\UrlBigBreakPenalty{0}
\hyphenpenalty=10000
\exhyphenpenalty=10000
\emergencystretch=3em
\AtBeginDocument{\def\UrlBigBreaks{}\def\UrlNoBreaks{\do\(\do\[\do\{\do\<\do\:}}
\usepackage{booktabs}

\newcommand{\CP}{\mathbb{CP}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\pione}{\pi_1}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}

\title{Independent machine verification of the fundamental-group
computation in Wuebben's exotic $S^2\times S^2$}
\author{John Clyde\\ \small VentiMath\\
\small \texttt{verification@ventimath.org}}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
Wuebben (arXiv:2608.17267), building on Lidman--Piccirillo
(arXiv:2505.14387), proves that a specified Lidman--Piccirillo piece $V$
--- a symplectic 4-manifold with the homology of $S^2\times D^2$, built
from a non-product genus-2 surface bundle over a once-punctured torus by
two Luttinger surgeries --- is simply connected; the symplectic double of
$V$ is then homeomorphic but not diffeomorphic to $S^2\times S^2$, and
Lidman--Piccirillo regluing yields an exotic
$\CP^2\#\overline{\CP}{}^2$. The critical step is the simple
connectivity: the triviality of the filled fundamental groups for the
paper's permitted surgery parametrizations. We report an independent
implementation of that step. The bundle is rebuilt as a simplicial
complex from the paper's marked-fiber data alone; the surgery tori, based
meridians, and framed longitudes are extracted combinatorially; and the
four theorem-critical filled presentations are proved trivial by complete
confluent rewriting, each computation emitting a replayable derivation
certificate that two independently written checkers accept. The framing
lemma connecting those combinatorial longitudes to the Lagrangian framing
is machine-checked with its analytic normalizations rebuilt
constructively. Stated carefully: the simple-connectivity proof appears
complete relative to explicitly named standard topology theorems, with no
known project-specific gap. The theorem is Wuebben's; this note claims
only the verification. All certificates, code, and provenance are public
in a pinned release.
\end{abstract}

\section{Introduction}

Claimed constructions of small exotic 4-manifolds have repeatedly failed
in one place: the fundamental-group computation. The manifolds of
\cite{Wuebben} rest on a specified Lidman--Piccirillo piece $V$, obtained
by two Luttinger surgeries \cite{Luttinger,ADK} on Lagrangian tori in a
symplectic non-product genus-2 surface bundle $R$ over a once-punctured
torus, following the framework of \cite{LidmanPiccirillo}. Exoticness of
the double of $V$ rests on $V$ being simply connected: the filled
fundamental groups of the paper's permitted surgery parametrizations ---
the four sign choices of the double surgery in the paper's principal
coordinate system --- must be trivial.

This note reports an independent implementation of that step, together
with a machine audit of the framing lemma that connects the group theory
to the symplectic geometry. Nothing in the implementation reuses the
paper's coordinates, code, or intermediate data: the geometry was rebuilt
from the paper's stated marked-fiber data alone, and the paper's
displayed relations, sign tables, and Table~1 filling words were then
\emph{compared against} the rebuild rather than assumed. The downstream
deductions --- from simple connectivity to the homeomorphism
classifications and the exoticness conclusions --- are Wuebben's; this
note verifies the simple-connectivity input and its geometric framing
bridge.

The result separates into an unconditional computational theorem and an
identification that connects it to the paper.

\begin{theorem}[certified triviality]\label{thm:main}
Let $P_{\epsilon_a\epsilon_b}$, for $(\epsilon_a,\epsilon_b)\in\{\pm\}^2$,
be the four filled presentations of Section~\ref{sec:groups}: the
exported 4-generator, 95-relator presentation of the complement of the
two surgery tori in the simplicial model, together with the two filling
relators of the $n{=}0$ double surgery with signs
$(\epsilon_a,\epsilon_b)$, imposed with the certified based meridians and
framed longitudes. Each $P_{\epsilon_a\epsilon_b}$ presents the trivial
group. The proof is a derivation certificate, contained in release
\texttt{v1.4} of the verification repository, that replays from a fresh
checkout and is accepted by two independently written checkers.
\end{theorem}

\begin{lemma}[certificate soundness]\label{lem:sound}
A certificate is a finite sequence of records, each an input relator, an
inverse axiom of the free group, a critical-pair overlap of two earlier
records, or a rewrite of an earlier record by earlier records, ending in
the rules $g\to 1$ for every generator $g$. A checker accepts only if
every record is derived as stated from records preceding it. Every
accepted record is then an equality holding in the group presented by
the input relators, so acceptance implies that this group is trivial.
Neither the confluence of the completed rewriting system nor the
correctness of the program that produced it enters the argument.
\end{lemma}

\begin{proposition}[identification]\label{prop:ident}
The presentations of Theorem~\ref{thm:main} present $\pione(V)$ for the
paper's four permitted parametrizations: the simplicial model is
identified with the paper's marked bundle by a constructed fiberwise
homeomorphism (Section~\ref{sec:model}); its peripheral loops are based
meridians and product-framed longitudes of the surgery tori
(Section~\ref{sec:peripheral}); and the product framing agrees with the
Lagrangian framing of Luttinger surgery by the paper's Lemma~8.2,
machine-checked (Section~\ref{sec:peripheral}).
\end{proposition}

Theorem~\ref{thm:main} is unconditional: it is a finite object that any
reader can re-verify. Proposition~\ref{prop:ident} is where the residual
risk of any machine verification lives, since a machine certifies
properties of a model; it rests on the reading of the paper's
conventions, on elementary inputs of PL topology, and on the named
theorems of Section~\ref{sec:trust}. The accurate summary of its status
is: \emph{the simple-connectivity proof appears complete relative to
explicitly named standard topology theorems, with no known
project-specific gap} --- a statement about verification depth, offered
to facilitate expert review, not to replace it.

Two disclosures. The theorem is Wuebben's; this note asserts nothing
about priority or authorship of the mathematics and claims only the
verification. The verification was carried out by generative-AI systems
under the author's direction, with commit-level provenance recorded in
the repository's ledger and the corrections produced by adversarial
cross-model review preserved rather than rewritten; mathematical
responsibility rests with the human author. No claim below depends on
trusting a model: every asserted computation ships with a replayable
certificate.

\section{The independent model and the interpretation
dictionary}\label{sec:model}

The genus-2 fiber is realized as a regular neighborhood of the paper's
five-chain of curves --- five plumbed annular bands and two cone discs
--- with the hyperelliptic-type involution $\varphi_0$ realized
simplicially; its fixed points, the curve actions $a\leftrightarrow e$,
$b\leftrightarrow d$, and the free half-rotation of the middle curve $c$
are executable assertions about the complex, not assumptions. The bundle
$R$ is assembled from simplicial mapping cylinders over the once-punctured
torus base; the two surgery tori $T_\alpha$, $T_\beta$ arise as induced
subcomplexes, and the assembled 4-complex passes manifold checks down to
the vertex-link level.

The identification with the paper's marked bundle is by literal based
monodromy action: the based mapping classes of both monodromies are
certified as equalities of vertex paths, transported by proof-producing
Tietze chains that replay step by step, and completed by an explicit
marked mapping-cylinder clutching over the base graph. The identification
is thus a constructed fiberwise homeomorphism into the paper's bundle
rather than an appeal to the classification of surface bundles.
Everything the surgery argument needs is transported through that map
into the paper's already-smooth manifold, where the smooth and symplectic
arguments run; no smoothing of the triangulation is ever chosen.

Because the machine certifies properties of a model, the model itself is
the residual risk. Five structurally different implementations
reconstruct it independently, arranged so that each class of translation
error --- subdivision artifacts, monodromy transcription, ordering, sign,
and orientation conventions --- would be caught by at least one of them.
What redundancy cannot exclude, a shared misreading of the paper's
conventions, is addressed by a bound interpretation dictionary: every
convention read from the paper carries the certified computation that
would fail under a rival reading. Wuebben's committed scripts enter at
exactly one point, parsed and never executed: after the dictionary was
frozen, selected entries were compared diagnostically against them, and
no verified conclusion consumes script-derived data. The remaining
transcription-level entries are audited by a quarantined extractor
against the hash-pinned immutable Lidman--Piccirillo v1 arXiv source
\cite{LidmanPiccirillo}, in both its prose and its vector-figure layers;
the one interpretation remaining at that boundary is the ordinary
convention that dashed arcs in a handle picture depict hidden projection
rather than extra crossings. The full inventory of implementations and
the dictionary are in the repository.

\section{Peripheral data and the framing
identification}\label{sec:peripheral}

Based meridians and product-framing longitudes for both tori are traced
as literal simplicial loops carrying the paper's own basing whiskers. The
paper's sign-correction package (its \S7.4 fix) is reproduced
independently, and a combinatorial sweep of the relevant transport square
resolves the basing double-count question in the direction the paper's
corrected convention requires. Both surgery tori are certified locally
flat at every simplex link, and every extracted dual meridian is checked
to be a literal normal-circle fiber of an explicit normal block model,
with no regular-neighborhood theorem invoked.

Luttinger surgery is performed with respect to the Lagrangian framing;
the paper's Lemma~8.2 identifies it with the fibered framing carried by
the combinatorial longitudes above. This lemma carries an exact logical
load: if the identification failed by $j$ meridians, the certificates of
Theorem~\ref{thm:main} would remain correct for the product-framed
presentations they literally describe, but would no longer identify
those presentations with the paper's surgeries. The lemma's displayed
Weinstein-chart, seam, and constant-momentum algebra are machine-checked
symbolically, and its inline analytic normalizations are rebuilt
constructively rather than cited: the relative Moser flow is produced as
the explicit monotone inverse of a cumulative coordinate, and the
equivariant step defines the Moser field directly on the double cover
with certified projection and deck invariance, so neither an ODE
existence theorem nor a covering-lift theorem is invoked. Chart
independence is proved between the paper's displayed charts by two
routes, with the one symplectic move that could introduce meridian
components --- the closed-1-form shift --- excluded by the zero-section
condition; \cite{ADK} (\S2.1 and Proposition~2.2) stands as
corroboration.

Independently of the lemma, a finite scan tests what a framing error
would do. It refills the complement with longitudes shifted by
$(j_a,j_b)$ meridians on $(T_\alpha,T_\beta)$ for the eight axis shifts
$(j_a,0)$, $(0,j_b)$ with $0<|j|\le 2$ and the four diagonal shifts
$(\pm1,\pm1)$ --- a cross-and-diagonal sample, not the full grid ---
across all four sign pairs and both half-drift families, giving 96
counterfactual presentations, plus four zero-shift controls that
re-derive the paper's own $n{=}0$ fillings through the same pipeline. All
100 present the trivial group, and no nontriviality witness was found at
any stage. The evidence is of two levels. The scan pipeline, which
simplifies each presentation before attempting completion, decided 82
cases at the level of a GAP session --- 40 by Tietze collapse of the
simplified presentation and 42 by its confluent completion --- and those
runs exported no certificates; the four zero-shift controls among them
are the paper's own fillings, whose separate certificates are those of
Theorem~\ref{thm:main}. The 18 it could not decide, every one involving a
shift on $T_\alpha$, carry derivation certificates of the kind in
Section~\ref{sec:certificates}: 17 from confluent completion of the
unsimplified 97-relator presentation, whose redundant relators are what
make completion tractable, and the last, the $n{=}0$ $(+,+)$ filling
shifted by $(+1,+1)$, from a certified reduction through the 96 relators
it shares with its $n{=}1$ companion, which complete confluently to an
infinite cyclic group in which the omitted relator reduces to a
generator. Within the scanned range, then, no framing error would have
produced a nontrivial group. The scan is robustness evidence only: the
paper's own fillings are the certified controls, and it bears on the
proof only if Lemma~8.2 first fails. The table, methods, and search
history are in the repository.

\section{Certified triviality of the four theorem-critical
groups}\label{sec:groups}

Filling the triangulation-derived complement presentation (4 generators,
95 relators) with the certified coherent peripheral pairs yields, for all
four $n{=}0$ sign pairs, complete confluent rewriting systems in which
every generator reduces to the identity. Each completion is exported as a
derivation certificate, and Theorem~\ref{thm:main} is the statement that
the four certificates verify.

\begin{center}
\begin{tabular}{llr}
\toprule
system & signs & derivation records\\
\midrule
$n{=}0$ & $(-,-)$ & 1123\\
$n{=}0$ & $(-,+)$ & 1075\\
$n{=}0$ & $(+,-)$ &  804\\
$n{=}0$ & $(+,+)$ & 1504\\
\bottomrule
\end{tabular}

\smallskip
\small Derivation-DAG certificates for the four theorem-critical filled
groups; each verifies \textsc{trivial} from a fresh clone of the
repository.
\end{center}

Four adjacent $n{=}1$ half-drift presentations of the same fillings ---
re-indexings that carry no independent logical load --- pass the
identical certification as an auxiliary check; their certificates sit
beside these in the same manifest.

\section{Certificate verification}\label{sec:certificates}

Knuth--Bendix completion (KBMAG \cite{KBMAG}, run under GAP \cite{GAP})
is used only as an \emph{untrusted certificate generator}. A patched
build of KBMAG records the ancestry of every equation it derives; an
untrusted compiler prunes that history to the dependency cone of the
final identity rules and emits a certificate of the form in
Lemma~\ref{lem:sound}, binding the input presentation by SHA-256. A small
independent checker then re-verifies every input-relator axiom, inverse
axiom, critical overlap, rewrite trace, tidying change, and final
generator-to-identity rule; it neither imports nor runs KBMAG. A second
checker, written independently in Ruby, re-verifies all retained
derivation records against the same hash-bound certificates and accepts,
importing neither the first checker nor any project group code. All
certificates, the exported rewriting systems, and a hash manifest replay
from a clean checkout, and the stored rewriting systems reload and
re-verify under GAP/KBMAG independently of the certificate path. The
design principle throughout is that certificates are checked, not
trusted: search and completion tools are untrusted generators, and every
computational claim rests on a replayable artifact validated by small
independent code.

\section{Trust boundary and data availability}\label{sec:trust}

\subsection*{Inputs to the verification result}

Theorem~\ref{thm:main} has no mathematical input beyond
Lemma~\ref{lem:sound}; its trust rests on the two small checkers.
Proposition~\ref{prop:ident} rests on elementary inputs of PL topology
retained as standard without a specific citation --- the
ribbon-thickening and bistellar-trace interpretations that read the
paper's figures into complexes, the classification of surfaces, the
collapsible-3-ball and cyclic-knot-unknot criteria behind the
local-flatness certificates, and simplicial fundamental-group
presentation with Tietze theory --- together with the Ker\'ekj\'art\'o
periodic-disk involution theorem \cite{ConstantinKolev} behind the disk
extension of the paper's Lemma~7.1, and \cite{ADK} as corroboration of
the framing identification. The ribbon-interpretation input is narrower
than the phrase suggests: the certified classification of equivariant
ribbon rotations pins the implication from the paper's stated marked
five-chain data to the equivariant normal form, and the stated data
themselves are audited in the original Lidman--Piccirillo source, so what
this boundary retains is the thickening of that data into complexes
together with the dashed-arc convention of Section~\ref{sec:model}. This
note reproves none of these.

\subsection*{Downstream theorems audited, not depended on}

The deductions from simple connectivity to exoticness are Wuebben's and
are inputs to nothing above. Their named inputs --- Freedman's simply
connected classification \cite{Freedman} with the Hambleton--Kreck
finite-cyclic classification \cite{HK88,HK93}; symplectic Kodaira
dimension and minimality via the Luttinger-surgery constructions
\cite{ADK,HoLi}; the symplectic Thom theorem \cite{OzsvathSzabo}; and the
Rokhlin/Arf input of the slicing argument together with the
Ozsv\'ath--Szab\'o / Lidman--Piccirillo Floer machinery
\cite{LidmanPiccirillo} --- were audited against their hypotheses, and no
gap was found. That audit is recorded in the repository and is not part
of this note's claim.

\subsection*{Data availability}

All permanent certificates, frozen inputs, replay scripts, hash
manifests, run transcripts, and provenance records are contained, with
clean-checkout replay instructions, in the tagged release \texttt{v1.4}
of the repository:
\url{https://github.com/VentiMath/exotic-s2xs2-verification/releases/tag/v1.4}.
The \texttt{verification/} tree of that release is identical to that of
the repository commit
\begin{center}\small\texttt{3f269d3cceeb84d04b3e2bfcc1c5875f55a5112f}\end{center}
and its filled-group proof manifest has SHA-256
\begin{center}\small\texttt{c79ab0fe97049a7e437f540b274d6425d0bfae2d5a53753e2c349c5351f92da5}\end{center}
The raw completion histories from which certificates are compiled are
reproducible build products and are omitted. The paper's own code is at
\url{https://github.com/bwuebben/exotic-s2xs2}; nothing in the
verification derives from it except where explicitly cited.

\begin{thebibliography}{99}

\bibitem{Wuebben}
B.~J.~Wuebben,
\emph{An exotic $S^2\times S^2$ and an exotic
$\CP^2\#\overline{\CP}{}^2$},
arXiv:2608.17267 (2026).

\bibitem{LidmanPiccirillo}
T.~Lidman and L.~Piccirillo,
\emph{Distinguishing closed 4-manifolds by slicing},
arXiv:2505.14387 (2025).

\bibitem{ADK}
D.~Auroux, S.~K.~Donaldson, and L.~Katzarkov,
\emph{Luttinger surgery along Lagrangian tori and non-isotopy for singular
symplectic plane curves},
Math. Ann. \textbf{326} (2003), 185--203.

\bibitem{ConstantinKolev}
A.~Constantin and B.~Kolev,
\emph{The theorem of Ker\'ekj\'art\'o on periodic homeomorphisms of the
disc and the sphere},
Enseign.\ Math.\ (2) \textbf{40} (1994), 193--204.

\bibitem{Luttinger}
K.~M.~Luttinger,
\emph{Lagrangian tori in $\R^4$},
J. Differential Geom. \textbf{42} (1995), 220--228.

\bibitem{Freedman}
M.~H.~Freedman,
\emph{The topology of four-dimensional manifolds},
J. Differential Geom. \textbf{17} (1982), 357--453.

\bibitem{HK88}
I.~Hambleton and M.~Kreck,
\emph{On the classification of topological 4-manifolds with finite
fundamental group},
Math. Ann. \textbf{280} (1988), 85--104.

\bibitem{HK93}
I.~Hambleton and M.~Kreck,
\emph{Cancellation, elliptic surfaces and the topology of certain
four-manifolds},
J. Reine Angew. Math. \textbf{444} (1993), 79--100.

\bibitem{HoLi}
C.-I.~Ho and T.-J.~Li,
\emph{Luttinger surgery and Kodaira dimension},
Asian J. Math. \textbf{16} (2012), 299--318.

\bibitem{OzsvathSzabo}
P.~Ozsv\'ath and Z.~Szab\'o,
\emph{The symplectic Thom conjecture},
Ann. of Math. (2) \textbf{151} (2000), no.~1, 93--124.

\bibitem{KBMAG}
D.~F.~Holt,
\emph{KBMAG --- Knuth--Bendix in Monoids and Automatic Groups},
software package, version 1.5.9.

\bibitem{GAP}
The GAP~Group,
\emph{GAP --- Groups, Algorithms, and Programming},\newline
\url{https://www.gap-system.org}.

\end{thebibliography}

\end{document}
