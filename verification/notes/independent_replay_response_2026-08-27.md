# Response to the independent replay review (2026-08-27)

An independent session replayed Runs 48--52 and the proof ledger successfully,
then adversarially reviewed their claims. Its substantive findings were
incorporated as follows.

1. **Auxiliary scan count corrected.** The frozen Run-45 result is 82
   certified trivial, 18 inconclusive, zero nontrivial. The contradictory
   83/17 sentence in `STATUS.md` was corrected. No scan artifact changed.

2. **Weinstein boundary clarified.** The stale status sentence claiming that
   Lagrangian-neighborhood germ uniqueness remained external was removed.
   Run 43's geometric argument applies the fiber-dilation germ isotopy to two
   already-given charts and proves precisely the constant-momentum push-off
   independence needed here. The proof ledger records this as the geometric
   node `G_weinstein_chart_independence`, supported by its first-jet machine
   check. ADK03 is corroborating, not an undeclared theorem dependency.

3. **Mapping-cylinder convention repaired.** For
   `M_f=(F x I)/((x,1)~(f(x),0))`, the required equation is
   `g k_1=k_0 f`. The alternate-chart fallback now uses
   `k_s=g^-1 J_(1-s) h`. The checker formally reduces both sides to the same
   word `h f`, so the former transposition is now a tested failure mode. The
   exact-conjugacy main line was unaffected.

4. **Run 52 weighted and declared honestly.** Status and Run 52 now call it
   a symbolic assembly certificate plus an elementary quotient argument, not
   an independent large-scale geometric computation. The ledger edges for
   Run 34's marked fiber, the rotation-system thickening, Run 51, and the
   labeled flip interpretation are attached directly to the topological
   reroute node as well as to the final marked-bundle node.

5. **Interpretation risk restored.** The risk register again names the main
   project-specific attack surface: the reading of `psi_0=T_a o T_b`, twist
   signs, the marked ribbon dictionary, and named whiskers. The based-action
   replay is strong corroboration, not a reason to hide this translation
   layer.

6. **Double and housekeeping gaps fixed.** The Run-50 note now states how the
   fiberwise map is applied to both halves and descends across the doubled
   boundary seam. The status date is current. Reconciliation of any separate
   release repository or arXiv-facing draft remains a release task; this
   repository's active README, status, notes, certificates, and ledger are
   internally synchronized.

The review's positive assessment of Runs 48--50 is unchanged. The corrections
above narrow claims where appropriate and repair the one actual algebra error;
they do not alter the certified `j=0` filled presentations or their triviality
proofs.
