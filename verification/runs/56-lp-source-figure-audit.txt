# Run 56: audit the original Lidman--Piccirillo Figure 1 source (2026-08-27)

Retrieve and extract the immutable source:

```
curl -L https://arxiv.org/e-print/2505.14387v1 -o lp2505v1-source.tar
mkdir lp2505v1-source
tar -xf lp2505v1-source.tar -C lp2505v1-source
```

Run:

```
python3 luttinger/lp_source_figure_audit.py \
  --source-dir lp2505v1-source \
  --check
```

Result:

```
PASS: lp_source_figure_certificate.json exactly reproduced
PASS: immutable LP v1 source hashes match
PASS: a-b-c-d-e is the ordered five-chain in the original vector figure
PASS: original prose and vector layer give the chain-reversing involution
PASS: exactly two fixed points appear both in prose and vector data
PASS: original LP source supplies every Run-55 marked input
```

The checker reads only `main.tex` and `morecurves.pdf` from the original
Lidman--Piccirillo source. It imports no project module. The original files
are hash-bound but are not redistributed in this repository.

Certificate SHA-256:

```
caed38db3088266751c716fd1662ee02ba4a2c094bf2c0b6d203e6d3d670d4b6
```

Detailed note:

`notes/lp_source_figure_audit_2026-08-27.md`
