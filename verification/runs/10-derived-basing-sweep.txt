Command:
  PYTHONHASHSEED=0 PATH=~/luttinger/bin:$PATH python3 r_run.py

Certified geometric data:
  oriented K and both torus components
  beta basing sweep: grid 7x35, alpha hits=1, beta boundary hits=34;
  oriented intersection sign=+1
  punctured sweep boundary lengths: [6, 34, 34]

The sign is relative to the deterministically propagated component
orientations; reversing either K or T_alpha reverses both the meridian
orientation and the intersection sign, leaving the emitted correction word
unchanged.

Every horizontal and vertical grid edge is asserted to be an edge of K, and
every grid cell is asserted to contain exactly two triangles of K.  The arc
direction is selected by the normal rail used by side_b: its penultimate row
is exactly e_rail[0].  The unique T_alpha incidence has a full simplicial
collar, and every T_beta incidence is in the terminal row.  Deleting all grid
triangles incident to either target torus produces an oriented boundary chain
which is asserted to be a disjoint union of three circles.

Tracked words after the elementary Tietze reduction (3 generators, 78
relators) include the directly computed longitude lb_b, the independently
traced terminal boundary lb_b_sweep, the oriented crossing word corr_b, and
beta_reference.  The decisive comparison is

  inverse(lb_b) * lb_b_sweep = []

after the recorded Tietze substitutions.  Thus the direct longitude already
contains the basing effect represented geometrically by the sweep.  An extra
meridian factor must not be multiplied into lb_b.

The separate paper-coordinate diagnostic

  corr_b * beta_reference = lb_b_sweep

did not reduce syntactically; its residual had length 52.  This does not prove
inequality in the presented group.  It means the chosen dual meridian and
hand-assembled reference whisker have not yet been identified with the other
two traced boundary cycles.  That expression remains diagnostic only.

GAP certificate run (Docker GAP 4.11.1):
  Prop 3.5 model fingerprint = pi1(K) machine fingerprint
    = [[0,0],[1,3,7,26]]
  pi1(C) and all eight drilled sign variants have the same weak IDX=4
    fingerprint (see STATUS.md caveat)
  all eight direct-longitude surgery cases: H1=0, 2M coset enumeration OVERFLOW
  sweep-longitude cases are algebraically redundant after the exact comparison
    and the current generated GAP certificate does not enumerate them again.

Regression checks after the implementation:
  Python compileall: PASS
  fast_tietze: PASS (865 -> 4 generators)
  fiber checks: PASS
  twist-layer mapping-torus test: PASS
  T4 GAP fingerprints: match Z^2 x F2, Z^4, and expected surgeries
  Baldridge-Kirk GAP fingerprints: opposite signs tr(A)=1; equal signs tr(A)=3
