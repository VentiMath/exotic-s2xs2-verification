Run 15: explicit paper-based T_alpha filling direction (2026-08-23)
==================================================================

Supersession notice (2026-08-29)
--------------------------------

This historical run reached the correct conclusion but did not leave valid
evidence for it over either committed export.  Its claimed empty residual was
computed in a pre-export coordinate state and does not reproduce from the
sealed presentation.  In that presentation the literal target freely reduces
to a 72-letter word.  Run 68 now proves that exact 72-letter word equal to the
identity in the 3-generator, 78-relator complement, with a 2,506-record KBMAG
ancestry certificate independently replayed by Python and Ruby.  All claims
below about what Run 15 itself certified must be read as superseded by Run 68.

Question
--------
Does the triangulation-derived base-direction longitude on T_alpha equal the
paper's y_1-side, n=0 word A x, with all basepoints and orientations explicit?

Construction
------------
The earlier lb_a1/lb_a2 loops started at an arbitrary c[0] corner and used the
presentation tree as their basing.  That is sufficient for the internally
consistent surgery presentations but not for comparison with Section 8.4.

r_run.py now also constructs two paper-coordinate sections which:

* start at the unique c_y intersection on the explicit based y rail;
* use the literal initial segment y_1 from p to c_y as whisker;
* include a two-edge normal jog in C to the retracted boundary corner;
* lift the base generator A, not the displayed positive alpha loop (A is its
  inverse); and
* retain both closing half-arcs of c as separate y1/y2 candidates.

Before correcting the base orientation, the run reported the expected control:
the positive alpha lift was A^-1 x.  After reversing the lift, the full
Tietze pass in that pre-export coordinate state reported

  exact paper-coordinate candidate identities:
      coord_alpha_base_y1_A+1_x+1

The run therefore recorded the residual as the empty word:

  lb_a_y1^-1 * A * x = 1.

Conclusion
----------
The explicit y_1-side base-direction push-off is exactly A x in pi_1(C), as
now established by the complement-only certificate of Run 68.  Run 15 alone
does not establish that equality.  The adjacent half-section remains a
separate diagnostic and is not silently identified with A x.
